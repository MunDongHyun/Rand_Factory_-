# 개발 환경에서는 vite.config.js의 proxy(`/api` -> http://localhost:8000) 가 동작하므로
# 비워둬도 된다 (api.js는 미설정 시 `/api` 사용).
#
# 배포 환경(Vercel 등)에서는 백엔드 도메인을 명시:
# VITE_API_BASE_URL=https://articulum-api.onrender.com/api
VITE_API_BASE_URL=
