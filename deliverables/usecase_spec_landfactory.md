# landfactory 유스케이스 명세서

프로젝트명: landfactory  
개정 기준: 2026.05.22 현재 프로젝트 구조

## UC-100

| 항목 | 내용 |
|---|---|
| 유스케이스 이름 | 회원가입 |
| 유스케이스 ID | UC-100 |
| 관련 요구사항 | FR-US-100, FR-US-101, FR-US-105 |
| 우선순위 | 하 |
| 선행조건 | 사용자가 회원가입 화면에 접근한 상태 |
| 관련 액터 | 일반 회원(c), 학습자(j) |
| 이벤트 흐름 | 1. 사용자가 이름, 이메일, 비밀번호, 회사명, 초대 코드를 입력한다.<br>2. 시스템은 이메일 중복 여부를 확인한다.<br>3. 초대 코드가 없으면 일반 회원(c)으로 계정을 생성한다.<br>4. 초대 코드가 있으면 매니저 초대 코드의 유효성을 검증한다.<br>5. 유효한 초대 코드이면 학습자(j)로 계정을 생성하고 매니저의 회사명으로 저장한다.<br>6. 시스템은 회원가입 완료 결과를 반환한다. |
| 예외 흐름 | 이미 사용 중인 이메일이면 가입을 중단한다.<br>초대 코드가 올바르지 않거나 매칭되는 매니저가 없으면 가입을 중단한다.<br>필수 입력값이 누락되면 검증 메시지를 표시한다. |
| 종료조건 | 사용자 계정이 생성되고 역할이 저장됨 |

## UC-110

| 항목 | 내용 |
|---|---|
| 유스케이스 이름 | 로그인 및 세션 복원 |
| 유스케이스 ID | UC-110 |
| 관련 요구사항 | FR-US-102, FR-US-103 |
| 우선순위 | 하 |
| 선행조건 | 사용자가 가입되어 있고 탈퇴 처리되지 않은 상태 |
| 관련 액터 | 일반 회원(c), 학습자(j), 매니저(m), 관리자(a) |
| 이벤트 흐름 | 1. 사용자가 이메일과 비밀번호를 입력한다.<br>2. 시스템은 이메일과 비밀번호 해시를 검증한다.<br>3. 검증 성공 시 JWT access token을 발급한다.<br>4. 클라이언트는 `/api/users/me`를 호출해 현재 사용자 정보를 복원한다.<br>5. 사용자 역할에 따라 접근 가능한 메뉴와 화면을 표시한다. |
| 예외 흐름 | 이메일 또는 비밀번호가 일치하지 않으면 401 응답과 실패 메시지를 반환한다.<br>삭제된 계정이면 인증 대상에서 제외한다.<br>인증 만료 시 클라이언트는 자동 로그아웃 처리한다. |
| 종료조건 | JWT access token 발급 및 사용자 정보 복원 |

## UC-200

| 항목 | 내용 |
|---|---|
| 유스케이스 이름 | 아티클 목록 및 상세 조회 |
| 유스케이스 ID | UC-200 |
| 관련 요구사항 | FR-AR-100, FR-AR-102, FR-AR-103, FR-AR-104 |
| 우선순위 | 상 |
| 선행조건 | 로그인 완료 |
| 관련 액터 | 일반 회원(c), 학습자(j), 매니저(m), 관리자(a) |
| 이벤트 흐름 | 1. 사용자가 대시보드에서 아티클 목록 화면에 접근한다.<br>2. 시스템은 페이지, 카테고리, 출처 조건에 맞는 아티클 목록을 조회한다.<br>3. 시스템은 썸네일 URL, 요약 존재 여부, 미리보기 요약을 반환한다.<br>4. 사용자가 특정 아티클을 선택한다.<br>5. 시스템은 아티클 상세 정보와 저자 목록을 반환한다.<br>6. 상세 조회 시 조회 수와 사용자 활동 로그를 저장한다. |
| 예외 흐름 | 존재하지 않는 아티클 요청 시 404를 반환한다.<br>요약 데이터가 없으면 404를 반환한다.<br>인사이트 추출에 필요한 본문이 없으면 422를 반환한다. |
| 종료조건 | 아티클 목록 또는 상세 정보가 화면에 표시됨 |

## UC-210

| 항목 | 내용 |
|---|---|
| 유스케이스 이름 | 맞춤형 아티클 검색 및 추천 |
| 유스케이스 ID | UC-210 |
| 관련 요구사항 | FR-AR-101 |
| 우선순위 | 상 |
| 선행조건 | 로그인 완료 및 아티클 벡터 인덱스 존재 |
| 관련 액터 | 전체 사용자, AI/RAG 시스템 |
| 이벤트 흐름 | 1. 사용자가 검색어를 입력한다.<br>2. 시스템은 ChromaDB 벡터 검색을 수행한다.<br>3. 시스템은 아티클 제목 직접 매칭 결과를 추가 조회한다.<br>4. 시스템은 사용자의 최근 조회 카테고리 기반 선호 가중치를 계산한다.<br>5. 벡터 유사도, 직접 매칭, 선호 가중치를 병합해 정렬한다.<br>6. 관련 아티클 목록을 사용자에게 표시한다. |
| 예외 흐름 | 유사도 기준을 통과한 결과가 없으면 빈 목록을 반환한다.<br>벡터 검색 서비스 실패 시 서버 오류를 반환한다. |
| 종료조건 | 검색어와 관련된 아티클 목록이 반환됨 |

## UC-220

| 항목 | 내용 |
|---|---|
| 유스케이스 이름 | 북마크 관리 |
| 유스케이스 ID | UC-220 |
| 관련 요구사항 | FR-BM-100 |
| 우선순위 | 중 |
| 선행조건 | 로그인 완료 |
| 관련 액터 | 일반 회원(c), 학습자(j), 매니저(m), 관리자(a) |
| 이벤트 흐름 | 1. 사용자가 아티클에서 북마크 버튼을 클릭한다.<br>2. 시스템은 해당 아티클 존재 여부를 확인한다.<br>3. 기존 북마크가 없으면 사용자-아티클 북마크를 생성한다.<br>4. 사용자는 내 북마크 화면에서 저장된 아티클 목록을 조회한다.<br>5. 사용자가 북마크 해제를 선택하면 시스템은 해당 북마크를 삭제한다. |
| 예외 흐름 | 존재하지 않는 아티클이면 404를 반환한다.<br>이미 존재하는 북마크 생성 요청이면 기존 북마크 정보를 반환한다. |
| 종료조건 | 사용자별 북마크 목록이 갱신됨 |

## UC-230

| 항목 | 내용 |
|---|---|
| 유스케이스 이름 | 저자 조회 및 이메일 발송 |
| 유스케이스 ID | UC-230 |
| 관련 요구사항 | FR-AU-100, FR-AU-101, FR-AU-102 |
| 우선순위 | 중 |
| 선행조건 | 로그인 완료 |
| 관련 액터 | 전체 사용자, 이메일 서비스 |
| 이벤트 흐름 | 1. 사용자가 저자 이메일링 화면 또는 아티클 상세 화면에서 저자 정보를 조회한다.<br>2. 시스템은 저자명, 소속, 이메일, 관련 카테고리, 관련 아티클을 반환한다.<br>3. 사용자가 이메일이 등록된 저자를 선택하고 제목, 본문, reply-to를 입력한다.<br>4. 시스템은 입력값을 검증하고 이메일 서비스로 발송 요청을 전달한다.<br>5. 발송 성공 여부를 사용자에게 표시한다. |
| 예외 흐름 | 저자가 존재하지 않으면 404를 반환한다.<br>저자 이메일이 없으면 발송을 중단한다.<br>이메일 서비스 설정 누락 시 503, 발송 오류 시 502를 반환한다. |
| 종료조건 | 저자 정보 조회 또는 이메일 발송 완료 |

## UC-400

| 항목 | 내용 |
|---|---|
| 유스케이스 이름 | AI 커리큘럼 생성 |
| 유스케이스 ID | UC-400 |
| 관련 요구사항 | FR-CU-100 |
| 우선순위 | 상 |
| 선행조건 | 로그인 완료 및 사용자 역할이 매니저(m) 또는 관리자(a) |
| 관련 액터 | 매니저(m), 관리자(a), AI/RAG 시스템 |
| 이벤트 흐름 | 1. 매니저 또는 관리자가 과정명, 대상 직무, 대상 산업, 교육 기간, 학습 목표, 필수 내용을 입력한다.<br>2. 시스템은 입력값의 충분성과 유효성을 검증한다.<br>3. 시스템은 AI 커리큘럼 생성 서비스를 호출한다.<br>4. AI는 주차별 주제, 학습 목표, 과제, 추천 아티클, 코칭 가이드를 생성한다.<br>5. 시스템은 생성 결과를 화면에 표시한다. |
| 예외 흐름 | 사용자 역할이 m/a가 아니면 403을 반환한다.<br>입력값 검증 실패 시 400을 반환한다.<br>AI 생성 실패 시 502를 반환한다. |
| 종료조건 | 주차별 커리큘럼 초안이 생성됨 |

## UC-410

| 항목 | 내용 |
|---|---|
| 유스케이스 이름 | 커리큘럼 생성, 수정 및 학습자 배정 |
| 유스케이스 ID | UC-410 |
| 관련 요구사항 | FR-CU-101, FR-CU-102, FR-CU-103, FR-CU-104 |
| 우선순위 | 상 |
| 선행조건 | 매니저 또는 관리자가 커리큘럼 내용을 확정한 상태 |
| 관련 액터 | 매니저(m), 관리자(a), 학습자(j) |
| 이벤트 흐름 | 1. 매니저 또는 관리자가 커리큘럼 제목, 대상 직무/산업, 기간, 목표, 주차 계획을 저장한다.<br>2. 시스템은 사용자 역할을 확인한다.<br>3. 매니저가 학습자를 배정하는 경우 같은 회사의 활성 학습자인지 검증한다.<br>4. 시스템은 커리큘럼과 배정 학습자 ID 목록을 저장한다.<br>5. 학습자는 본인에게 배정된 활성 커리큘럼을 조회한다.<br>6. 매니저는 본인이 생성한 커리큘럼을 조회·수정한다.<br>7. 관리자는 전체 커리큘럼을 조회·수정한다. |
| 예외 흐름 | m/a가 아닌 사용자의 생성 요청은 403을 반환한다.<br>권한 없는 커리큘럼 조회/수정 요청은 404로 숨김 처리한다.<br>배정할 수 없는 학습자 ID가 포함되면 400을 반환한다. |
| 종료조건 | 커리큘럼 저장 및 배정 정보 반영 |

## UC-420

| 항목 | 내용 |
|---|---|
| 유스케이스 이름 | 커리큘럼 문서 내보내기 |
| 유스케이스 ID | UC-420 |
| 관련 요구사항 | FR-CU-105 |
| 우선순위 | 하 |
| 선행조건 | 커리큘럼 데이터가 생성되어 화면에 표시된 상태 |
| 관련 액터 | 매니저(m), 관리자(a) |
| 이벤트 흐름 | 1. 사용자가 커리큘럼 화면에서 내보내기 형식을 선택한다.<br>2. 시스템은 사용자 역할이 m/a인지 확인한다.<br>3. 시스템은 커리큘럼 데이터를 TXT, PDF 또는 DOCX 형식으로 변환한다.<br>4. 변환된 파일을 다운로드 응답으로 반환한다. |
| 예외 흐름 | m/a가 아닌 사용자는 404로 차단한다.<br>커리큘럼 데이터 형식이 올바르지 않으면 서버 오류 또는 검증 오류를 반환한다. |
| 종료조건 | TXT, PDF 또는 DOCX 파일이 다운로드됨 |

## UC-500

| 항목 | 내용 |
|---|---|
| 유스케이스 이름 | 과제 작성 및 제출 |
| 유스케이스 ID | UC-500 |
| 관련 요구사항 | FR-TS-100, FR-TS-101 |
| 우선순위 | 상 |
| 선행조건 | 학습자가 활성 커리큘럼에 배정되어 있음 |
| 관련 액터 | 학습자(j) |
| 이벤트 흐름 | 1. 학습자가 배정된 커리큘럼 상세 화면에 접근한다.<br>2. 시스템은 학습자가 해당 커리큘럼에 배정되어 있는지 확인한다.<br>3. 학습자는 주차별 과제 내용을 작성한다.<br>4. 시스템은 주차 번호가 커리큘럼 계획에 존재하는지 확인한다.<br>5. 시스템은 제출 유형, 제출 내용, 제출 시간을 저장한다.<br>6. 학습자는 내 제출물 목록을 조회한다. |
| 예외 흐름 | 학습자가 아닌 사용자가 제출하면 403을 반환한다.<br>배정되지 않은 커리큘럼이면 404를 반환한다.<br>존재하지 않는 주차이면 400을 반환한다. |
| 종료조건 | 주차별 과제 제출물이 저장됨 |

## UC-510

| 항목 | 내용 |
|---|---|
| 유스케이스 이름 | 첨부파일 업로드, 다운로드 및 삭제 |
| 유스케이스 ID | UC-510 |
| 관련 요구사항 | FR-TS-102, FR-TS-103 |
| 우선순위 | 중 |
| 선행조건 | 과제 제출물이 존재함 |
| 관련 액터 | 학습자(j), 매니저(m), 관리자(a) |
| 이벤트 흐름 | 1. 학습자가 본인 제출물에 파일을 첨부한다.<br>2. 시스템은 제출물 접근 권한과 학습자 본인 여부를 확인한다.<br>3. 시스템은 파일 크기가 20MB 이하인지 확인한다.<br>4. 시스템은 파일명을 안전한 이름으로 정리한다.<br>5. 시스템은 magic byte 기반 MIME을 탐지하고 실행 파일을 차단한다.<br>6. 시스템은 SHA-256 해시와 파일 메타데이터를 DB에 저장한다.<br>7. 실제 파일을 `server/uploads/task_attachments/{submission_id}` 경로에 저장한다.<br>8. 권한이 있는 사용자는 첨부파일을 다운로드한다.<br>9. 학습자는 본인 제출물의 첨부파일을 삭제할 수 있으며 DB에는 soft delete 처리한다. |
| 예외 흐름 | 파일이 비어 있으면 400을 반환한다.<br>20MB를 초과하면 413을 반환한다.<br>실행 파일 등 차단 MIME이면 415를 반환한다.<br>권한 없는 다운로드 또는 삭제 요청은 404 또는 403을 반환한다. |
| 종료조건 | 첨부파일 메타데이터와 실제 파일 상태가 갱신됨 |

## UC-600

| 항목 | 내용 |
|---|---|
| 유스케이스 이름 | 제출물 조회 및 피드백 |
| 유스케이스 ID | UC-600 |
| 관련 요구사항 | FR-TS-101, FR-TS-104 |
| 우선순위 | 상 |
| 선행조건 | 과제 제출물이 존재함 |
| 관련 액터 | 매니저(m), 관리자(a), 학습자(j) |
| 이벤트 흐름 | 1. 매니저는 본인이 생성한 커리큘럼의 제출물 목록을 조회한다.<br>2. 관리자는 전체 제출물을 조회할 수 있다.<br>3. 학습자는 본인 제출물 상세를 조회할 수 있다.<br>4. 매니저 또는 관리자가 제출물에 피드백, 점수, 상태를 입력한다.<br>5. 시스템은 피드백 시간과 재제출 요청 여부를 저장한다.<br>6. 학습자는 갱신된 피드백 상태를 확인한다. |
| 예외 흐름 | 권한 없는 제출물 조회는 404를 반환한다.<br>m/a가 아닌 사용자의 피드백 작성 요청은 403을 반환한다.<br>점수가 0~100 범위를 벗어나면 검증 오류를 반환한다. |
| 종료조건 | 제출물 조회 또는 피드백 저장 완료 |

## UC-700

| 항목 | 내용 |
|---|---|
| 유스케이스 이름 | 관리자/매니저 대시보드 |
| 유스케이스 ID | UC-700 |
| 관련 요구사항 | FR-US-104, FR-DA-100, FR-DA-101, FR-DA-102 |
| 우선순위 | 중 |
| 선행조건 | 로그인 완료 및 관리자 또는 매니저 권한 보유 |
| 관련 액터 | 매니저(m), 관리자(a) |
| 이벤트 흐름 | 1. 관리자는 전체 회원 수, 월 가입자 수, 인기 회사, 회원 목록을 조회한다.<br>2. 관리자는 카테고리별 아티클 조회 통계와 조회 타임라인을 확인한다.<br>3. 관리자는 커리큘럼 수, 활성 학습자 수, 제출 수를 확인한다.<br>4. 관리자는 회원 역할을 변경하거나 탈퇴/복구 처리한다.<br>5. 매니저는 같은 회사 학습자 목록과 본인 커리큘럼의 제출 현황을 확인한다.<br>6. 매니저는 본인 초대 코드를 확인하고 복사한다. |
| 예외 흐름 | 관리자 전용 통계에 비관리자가 접근하면 404를 반환한다.<br>마지막 관리자 계정을 강등하거나 탈퇴시키려 하면 400을 반환한다.<br>매니저가 다른 회사 학습자를 조회하려 하면 404로 숨김 처리한다. |
| 종료조건 | 통계 및 관리 정보가 화면에 표시됨 |

## UC-710

| 항목 | 내용 |
|---|---|
| 유스케이스 이름 | 관리자 아티클 등록 |
| 유스케이스 ID | UC-710 |
| 관련 요구사항 | FR-AR-105 |
| 우선순위 | 중 |
| 선행조건 | 관리자 로그인 완료 |
| 관련 액터 | 관리자(a), AI/RAG 시스템 |
| 이벤트 흐름 | 1. 관리자가 아티클 출처, 제목, 저자, 발행일, 카테고리, 원문 URL, 본문을 입력한다.<br>2. 시스템은 관리자 권한을 확인한다.<br>3. 시스템은 아티클 메타데이터를 DB에 저장한다.<br>4. 본문이 입력된 경우 RAG 서비스가 아티클 본문을 인덱싱한다.<br>5. 시스템은 생성된 아티클 정보를 반환한다. |
| 예외 흐름 | 관리자가 아닌 사용자는 403을 반환한다.<br>본문 인덱싱 실패 시 서버 오류를 반환한다. |
| 종료조건 | 아티클 메타데이터 저장 및 본문 인덱싱 완료 |
